spring:
  application:
    name: assignforce-discovery-server

server:
  port: 8761 #default port

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

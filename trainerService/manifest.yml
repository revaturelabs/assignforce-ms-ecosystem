---
instances: 2
memory: 300MB
applications:
  - name: trainerService
    domain: revatureapps.io
    host: trainer-service
    buildpack: https://github.com/cloudfoundry/java-buildpack.git
    path: target/trainerService-0.0.1-SNAPSHOT.jar
    services:
      - service-registry //registers with built in eureka discovery
    env:
      SPRING_PROFILES_ACTIVE: dev
